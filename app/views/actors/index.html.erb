<h2>All of the actors</h2>

<ul>
  <% @actors.each do |actor| %>
  <li><%= link_to actor.name, actor_path(actor) %></li>
  <% end %>
</ul><br><br>

<%= link_to "Create a new actor!", new_actor_path %>
